{
	"author": "Morning Cappuccino <KnockKnockWakeUp@gmail.com>",
	"description": "",
	"license": "MIT",
	"main": "app.js",
	"name": "valeras-backend-plus-react",
	"version": "1.0.0",
	"dependencies": {
		"cross-env": "^7.0.2"
	},
	"devDependencies": {
		"concurrently": "^5.2.0"
	},
	"scripts": {
		"client": "cd client && yarn start",
		"dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
		"server": "cd server && cross-env PORT=5000 nodemon server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}